# Node.js dependencies and build artifacts
node_modules/
npm-debug.log
.env # Very important: Contains sensitive environment variables!
dist/ # Often the output directory for transpiled/bundled code
build/ # Another common build output directory
logs/ # Log files generated by your application

# Operating System specific files
.DS_Store # macOS
Thumbs.db # Windows
.vscode/ # Visual Studio Code specific files (unless you want to share workspace settings)
.idea/ # IntelliJ / WebStorm IDE files

# Database-related (less common for direct commit, but good to be aware of)
# If you have local database files that shouldn't be committed (e.g., SQLite files for dev)
*.sqlite3
*.db
data/*.sqlite
dump.sql # Or other database dump files if they are local and temporary

# Test-related
coverage/ # Test coverage reports

# Temporary files
*.tmp
*.log
*.swp # Vim swap files
*~ # Emacs backup files

# Other
.nyc_output/ # for istanbul/nyc test coverage

.eslintcache # ESLint cache file

# Environment files
.env
.env.local
.env.development
.env.production

